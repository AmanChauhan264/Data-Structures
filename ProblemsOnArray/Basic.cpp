//  1.   If we declare an array in side the main but did not initialize it's values then there will be random or garbage values on each indexes.

// But if we declare an array globally then each index will contain zero as the element.

//  2.  The maximum number of indexes a array can hold is 10^6 inside the main.

// But 10^7 if it is declared globally.


